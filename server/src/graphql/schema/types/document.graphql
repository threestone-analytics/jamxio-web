
  type Document {
    _id: ID
    recordId: ID
    format: String
    source: String
    geometry: JSON
    url: String
    publisher: String
    publishedDate: Date
    documentType: DocumentType
  }

